## Packages
recharts | Data visualization for fitness stats
framer-motion | Smooth animations for XP bars and page transitions
clsx | Conditional class names
tailwind-merge | Merging Tailwind classes safely

## Notes
Tailwind Config - extend fontFamily:
fontFamily: {
  display: ["var(--font-display)"],
  body: ["var(--font-body)"],
  mono: ["var(--font-mono)"],
}
